# Frontend Shell References

Read these only when the task needs the detail.

## Reference Docs

- `docs/reference/frontend.md` for frontend stack, package commands, and file
  structure.
- `docs/reference/testing.md` for Playwright patterns.
- Feature-domain capsules for route-specific behavior.

## ADR Headings

- No frontend-shell-specific ADR is currently active. Load feature ADRs for the
  route or workflow being changed.
