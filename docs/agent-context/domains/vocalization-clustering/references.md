# Vocalization Clustering References

Read these only when the task needs the detail.

## Reference Docs

- `docs/reference/classifier-api.md` for retained labeling and training surfaces
  that still live under classifier/detection flows.
- `docs/reference/behavioral-constraints.md` for detection and vocalization
  label constraints.
- `docs/reference/data-model.md` for vocalization and clustering tables.
- `docs/reference/storage-layout.md` for dataset and clustering artifacts.
- `docs/reference/frontend.md` for frontend stack and conventions.

## ADR Headings

- ADR-042: Multi-label vocalization type classifier via binary relevance
- ADR-054: Read-time correction overlay for downstream consumers
