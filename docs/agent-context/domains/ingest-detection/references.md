# Ingest Detection References

Read these only when the task needs the detail.

## Reference Docs

- `docs/reference/classifier-api.md` for classifier, detection embedding,
  hyperparameter, autoresearch, and timeline export endpoints.
- `docs/reference/hydrophone-rules.md` for provider and extraction rules.
- `docs/reference/signal-processing.md` for detection feature/window details.
- `docs/reference/storage-layout.md` for detection and classifier artifacts.
- `docs/reference/behavioral-constraints.md` for detection, labeling, classifier,
  and window-selection constraints.

## ADR Headings

- ADR-047: Replace heuristic gain-step detection with PCEN timeline normalization
- ADR-055: Perch v2 as first-class classifier family (Approach A - surgical extension)
