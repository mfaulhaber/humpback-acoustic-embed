# Signal Timeline References

Read these only when the task needs the detail.

## Reference Docs

- `docs/reference/signal-processing.md` for parameters, windowing, and pipeline
  diagrams.
- `docs/reference/behavioral-constraints.md` for timeline rendering and
  compound component constraints.
- `docs/reference/frontend.md` for frontend stack and file structure.
- `docs/reference/runtime-config.md` for PCEN and playback settings.
- `docs/reference/storage-layout.md` for timeline artifacts.

## ADR Headings

- ADR-047: Replace heuristic gain-step detection with PCEN timeline normalization
