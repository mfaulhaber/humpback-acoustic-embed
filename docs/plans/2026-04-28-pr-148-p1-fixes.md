# PR 148 P1 Fixes Implementation Plan

**Goal:** Fix merged PR 148 regressions in HMM label distribution and exemplar retrieval.
**Spec:** Bug fix from post-merge code review findings; no separate design spec.

---

### Task 1: Correct label distribution timestamp domain

**Files:**
- Modify: `src/humpback/services/hmm_sequence_service.py`
- Modify: `tests/services/test_hmm_sequence_service.py`

**Acceptance criteria:**
- [x] Hydrophone-backed HMM state windows are joined to detection windows using absolute UTC center timestamps.
- [x] Relative state timestamps remain unchanged in stored HMM artifacts and API responses.
- [x] Label distribution still returns `unlabeled` when no labeled detection window contains the UTC center.

**Tests needed:**
- Service-level test where a region detection job has a non-zero `start_timestamp`, HMM state rows are relative seconds, detection row store rows are absolute UTC seconds, and `generate_label_distribution()` counts the manual vocalization label.

---

### Task 2: Allow hydrophone exemplars without audio file IDs

**Files:**
- Modify: `src/humpback/schemas/sequence_models.py`
- Modify: `frontend/src/api/sequenceModels.ts`
- Modify: `frontend/src/components/sequence-models/HMMSequenceDetailPage.tsx`
- Modify: `tests/unit/test_sequence_models_schemas.py`

**Acceptance criteria:**
- [x] `ExemplarRecord` accepts `audio_file_id = null` for hydrophone-backed sequence model jobs.
- [x] Existing exemplar payloads with integer audio file IDs remain valid.
- [x] Frontend API types and display handle nullable exemplar audio file IDs.

**Tests needed:**
- Schema test that validates an exemplar record with `audio_file_id=None`.

---

### Verification

Run in order after all tasks:
1. `uv run ruff format --check src/humpback/services/hmm_sequence_service.py src/humpback/schemas/sequence_models.py tests/services/test_hmm_sequence_service.py tests/unit/test_sequence_models_schemas.py`
2. `uv run ruff check src/humpback/services/hmm_sequence_service.py src/humpback/schemas/sequence_models.py tests/services/test_hmm_sequence_service.py tests/unit/test_sequence_models_schemas.py`
3. `uv run pyright src/humpback/services/hmm_sequence_service.py src/humpback/schemas/sequence_models.py tests/services/test_hmm_sequence_service.py tests/unit/test_sequence_models_schemas.py`
4. `uv run pytest tests/services/test_hmm_sequence_service.py tests/unit/test_sequence_models_schemas.py`
5. `uv run pytest tests/sequence_models/`
6. `cd frontend && npx tsc --noEmit`
